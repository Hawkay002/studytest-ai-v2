# Optional: shared demo key for a serverless proxy.
# When omitted, the app runs fully client-side with each user's own key.
# Get a free key at https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=
